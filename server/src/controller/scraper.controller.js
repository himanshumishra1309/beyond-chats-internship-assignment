import { ApiError } from "../utils/ApiError";
import { ApiResponse } from "../utils/ApiResponse";
import { asyncHandler } from "../utils/asyncHandler";
import { getFiveBlogs } from "../scripts/beyondChatScrape";

const scrapeBeyondChatArticles = asyncHandler(async(req, res)=>{
    try {
        
    } catch (error) {
        
    }
})